// 1.
let numero = 17

//2.
let tipoNumero = (numero>0) ? `El número ${numero} es positivo` : `El número ${numero} es cero o negativo`;
console.log(tipoNumero)

//3.
let cadena = "Hola, éste es el texto"

//4.
let longitudCadena = cadena.length > 5 ? `La cadena de texto "${cadena}", es larga` : `La cadena: "${cadena}", es corta`;
console.log(longitudCadena)